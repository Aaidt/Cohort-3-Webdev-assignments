const Header = ({ message }) => {
    return (
        <div style={{backgroundColor: " #abadab", padding: "20px", textAlign: "center"}}>
            <h1>{message}</h1>
        </div>
    )
}

export default Header